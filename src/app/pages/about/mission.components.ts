import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-mission',
  templateUrl: './mission.components.html',
  styleUrls: ['./mission.components.css'],
  imports: [RouterLink]
})
export class MissionComponents { }
